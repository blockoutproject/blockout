# Workspace Architecture Audit

This audit is read-only.

Inspect the repository map, Nx project graph, Maven reactor, Expo project, scraper targets, Docker build contexts,
central Compose files, root dependency ownership, ignored artifacts, CI paths, and documentation links.

Compare structural conventions with Maaatch while respecting Blockout technology differences. Classify each finding as
KEEP, FIX, SIMPLIFY, REVALIDATE, or BLOCK. Do not turn architectural preference into a production refactor.
