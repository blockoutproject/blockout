# Python contract clients

This private workspace package contains generated Blockout contract models and asynchronous HTTPX clients.

Each contract is generated into its own `blockout_contract_clients.<contract>` namespace. The shared namespace contains
reusable transport enums. Generated package directories are ignored and must never be committed.
