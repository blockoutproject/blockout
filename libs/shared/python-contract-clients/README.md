# Python contract clients

This private workspace package reserves the shared Python boundary for generated Blockout contract models and clients.
Code generation remains deferred to REF-041; generated sources must never be committed.
