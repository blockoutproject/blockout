{
  "asyncapi": "3.0.0",
  "channels": {
    "PoolDeactivation": {
      "$ref": "../shared/channels/lifecycle.json#/$defs/PoolDeactivation"
    },
    "PoolProjectionChanged": {
      "$ref": "../shared/channels/lifecycle.json#/$defs/PoolProjectionChanged"
    },
    "PoolUpsert": {
      "$ref": "../shared/channels/lifecycle.json#/$defs/PoolUpsert"
    }
  },
  "defaultContentType": "application/json",
  "info": { "title": "pools-service event contract", "version": "2.0.0" },
  "operations": {
    "consumePoolDeactivation": {
      "action": "receive",
      "channel": { "$ref": "#/channels/PoolDeactivation" }
    },
    "publishPoolUpsert": {
      "action": "send",
      "channel": { "$ref": "#/channels/PoolUpsert" }
    },
    "publishPoolProjectionChanged": {
      "action": "send",
      "channel": { "$ref": "#/channels/PoolProjectionChanged" }
    }
  },
  "x-blockout-primary-queues": ["pool.deactivation.queue.pools.v2"]
}
