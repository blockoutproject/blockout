{
  "RegisterPushTokenInternalRequest": {
    "type": "object",
    "description": "Validated Blockout push-registration command. Expo delivery messages, tickets, receipts, and provider configuration remain adapter-owned.",
    "required": ["expoPushToken", "platform", "deviceId"],
    "properties": {
      "expoPushToken": {
        "type": "string",
        "minLength": 1,
        "maxLength": 2048
      },
      "platform": { "$ref": "#/components/schemas/DevicePlatformEnum" },
      "deviceId": { "type": "string", "minLength": 1, "maxLength": 255 }
    },
    "additionalProperties": false
  }
}
