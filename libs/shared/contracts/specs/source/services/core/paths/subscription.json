{
  "/api/v2/users/me/subscription": {
    "get": {
      "operationId": "getCurrentSubscription",
      "tags": ["Subscription"],
      "description": "Current active profile only. Private, no-store. No caller-selected identity.",
      "responses": {
        "401": {
          "description": "Safe request failure",
          "content": {
            "application/problem+json": {
              "schema": {
                "$ref": "#/components/schemas/ProblemDetail"
              }
            }
          }
        },
        "403": {
          "description": "Safe request failure",
          "content": {
            "application/problem+json": {
              "schema": {
                "$ref": "#/components/schemas/ProblemDetail"
              }
            }
          }
        },
        "404": {
          "description": "Safe request failure",
          "content": {
            "application/problem+json": {
              "schema": {
                "$ref": "#/components/schemas/ProblemDetail"
              }
            }
          }
        },
        "409": {
          "description": "Safe request failure",
          "content": {
            "application/problem+json": {
              "schema": {
                "$ref": "#/components/schemas/ProblemDetail"
              }
            }
          }
        },
        "503": {
          "description": "Safe request failure",
          "content": {
            "application/problem+json": {
              "schema": {
                "$ref": "#/components/schemas/ProblemDetail"
              }
            }
          }
        },
        "default": {
          "description": "Safe request failure",
          "content": {
            "application/problem+json": {
              "schema": {
                "$ref": "#/components/schemas/ProblemDetail"
              }
            }
          }
        },
        "200": {
          "description": "Local evidence only; unknown does not mean free. Dates are UTC. No side effects.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SubscriptionResponse"
              }
            }
          }
        }
      }
    }
  },
  "/api/v2/users/me/subscription-refreshes": {
    "post": {
      "operationId": "refreshCurrentSubscription",
      "tags": ["Subscription"],
      "description": "Current active profile only. Private, no-store. No caller-selected identity.",
      "responses": {
        "401": {
          "description": "Safe request failure",
          "content": {
            "application/problem+json": {
              "schema": {
                "$ref": "#/components/schemas/ProblemDetail"
              }
            }
          }
        },
        "403": {
          "description": "Safe request failure",
          "content": {
            "application/problem+json": {
              "schema": {
                "$ref": "#/components/schemas/ProblemDetail"
              }
            }
          }
        },
        "404": {
          "description": "Safe request failure",
          "content": {
            "application/problem+json": {
              "schema": {
                "$ref": "#/components/schemas/ProblemDetail"
              }
            }
          }
        },
        "409": {
          "description": "Safe request failure",
          "content": {
            "application/problem+json": {
              "schema": {
                "$ref": "#/components/schemas/ProblemDetail"
              }
            }
          }
        },
        "503": {
          "description": "Safe request failure",
          "content": {
            "application/problem+json": {
              "schema": {
                "$ref": "#/components/schemas/ProblemDetail"
              }
            }
          }
        },
        "default": {
          "description": "Safe request failure",
          "content": {
            "application/problem+json": {
              "schema": {
                "$ref": "#/components/schemas/ProblemDetail"
              }
            }
          }
        },
        "202": {
          "description": "Durably accepted or coalesced. At most one effective user request per 30 seconds.",
          "headers": {
            "Location": {
              "schema": {
                "type": "string"
              },
              "description": "Subscription resource"
            },
            "Retry-After": {
              "schema": {
                "type": "integer"
              },
              "description": "Seconds before requesting another refresh"
            }
          }
        }
      }
    }
  }
}
