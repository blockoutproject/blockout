{
  "openapi": "3.0.3",
  "info": { "title": "Pool Service Internal API", "version": "1.0.0" },
  "servers": [{ "url": "http://localhost:8086" }],
  "tags": [{ "name": "Pool" }],
  "security": [{ "bearerAuth": [] }],
  "x-contract-schema-roots": [
    "CreatePoolInternalRequest",
    "UpdatePoolInternalRequest"
  ],
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    }
  }
}
