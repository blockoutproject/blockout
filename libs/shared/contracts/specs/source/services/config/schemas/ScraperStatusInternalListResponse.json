{
  "ScraperStatusInternalListResponse": {
    "type": "object",
    "description": "Complete two-scraper status catalog in the compatibility repository order retained by this migration slice.",
    "required": ["items"],
    "properties": {
      "items": {
        "type": "array",
        "maxItems": 2,
        "items": {
          "$ref": "#/components/schemas/ScraperStatusInternalResponse"
        }
      }
    },
    "additionalProperties": false
  }
}
