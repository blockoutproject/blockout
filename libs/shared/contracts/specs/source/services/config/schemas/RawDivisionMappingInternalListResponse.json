{
  "RawDivisionMappingInternalListResponse": {
    "type": "object",
    "description": "Complete raw-division mapping result for the optional exact league and season filters, in the compatibility repository order retained by this migration slice.",
    "required": ["items"],
    "properties": {
      "items": {
        "type": "array",
        "items": {
          "$ref": "#/components/schemas/RawDivisionMappingInternalResponse"
        }
      }
    },
    "additionalProperties": false
  }
}
