{
  "LegalDocumentInternalResponse": {
    "type": "object",
    "required": ["id", "type", "title", "version", "content"],
    "properties": {
      "id": { "type": "integer", "format": "int64" },
      "type": { "type": "string" },
      "title": { "type": "string" },
      "version": { "type": "string" },
      "content": { "type": "string" },
      "createdAt": {
        "type": "string",
        "format": "date-time",
        "nullable": true
      },
      "lastUpdate": {
        "type": "string",
        "format": "date-time",
        "nullable": true
      }
    }
  }
}
