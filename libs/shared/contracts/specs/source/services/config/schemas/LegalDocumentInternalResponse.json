{
  "LegalDocumentInternalResponse": {
    "type": "object",
    "description": "Legal document content selected by its stable type without persistence identity or audit fields.",
    "required": ["type", "title", "version", "content"],
    "properties": {
      "type": {
        "type": "string",
        "maxLength": 50
      },
      "title": {
        "type": "string",
        "maxLength": 255
      },
      "version": {
        "type": "string",
        "maxLength": 20
      },
      "content": {
        "type": "string"
      }
    },
    "additionalProperties": false
  }
}
