{
  "DivisionInternalListResponse": {
    "type": "object",
    "description": "Complete division catalog, including active and inactive rows, in the compatibility repository order retained by this migration slice.",
    "required": ["items"],
    "properties": {
      "items": {
        "type": "array",
        "items": {
          "$ref": "#/components/schemas/DivisionInternalResponse"
        }
      }
    },
    "additionalProperties": false
  }
}
