{
  "RawDivisionMappingInternalResponse": {
    "type": "object",
    "required": ["id", "rawDivisionName", "leagueCode", "season", "mapped"],
    "properties": {
      "id": { "type": "integer", "format": "int64" },
      "rawDivisionName": { "type": "string" },
      "divisionId": { "type": "integer", "format": "int64", "nullable": true },
      "format": { "$ref": "#/components/schemas/FormatEnum" },
      "gender": { "$ref": "#/components/schemas/GenderEnum" },
      "leagueCode": { "type": "string" },
      "season": { "type": "string" },
      "createdAt": {
        "type": "string",
        "format": "date-time",
        "nullable": true
      },
      "lastUpdate": {
        "type": "string",
        "format": "date-time",
        "nullable": true
      },
      "mapped": { "type": "boolean" }
    }
  }
}
