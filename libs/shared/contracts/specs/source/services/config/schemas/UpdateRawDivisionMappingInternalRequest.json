{
  "UpdateRawDivisionMappingInternalRequest": {
    "type": "object",
    "description": "Replaces all three mapping fields. An omitted or explicit-null field is stored as null, preserving the current unmapping behavior.",
    "properties": {
      "divisionId": {
        "type": "integer",
        "format": "int64",
        "minimum": 1,
        "nullable": true
      },
      "format": {
        "allOf": [
          {
            "$ref": "#/components/schemas/FormatEnum"
          }
        ],
        "nullable": true
      },
      "gender": {
        "allOf": [
          {
            "$ref": "#/components/schemas/GenderEnum"
          }
        ],
        "nullable": true
      }
    },
    "additionalProperties": false
  }
}
