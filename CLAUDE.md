# Blockout — Claude Code Instructions

Project skills are stored in `.agents/skills/*/SKILL.md`.

For Blockout repository work, start with:

```
.agents/skills/blockout-best-practices/SKILL.md
```

Use it as the project router. Load only the references required by the task. Blockout-specific production and
migration rules override generic framework examples when they conflict.
