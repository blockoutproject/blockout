{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "@blockout/competition-service",
  "targets": {
    "container": {
      "executor": "nx:run-commands",
      "cache": false,
      "inputs": ["default", "^default", "{workspaceRoot}/.dockerignore"],
      "dependsOn": [
        { "projects": "@blockout/contracts", "target": "generate-contracts" }
      ],
      "options": {
        "command": "docker build --build-arg APP_REVISION=0000000000000000000000000000000000000000 --file apps/backend/competition-service/Dockerfile --tag blockout-competition-service:local ."
      }
    },
    "migration-container": {
      "executor": "nx:run-commands",
      "cache": false,
      "inputs": ["default", "{workspaceRoot}/.dockerignore"],
      "options": {
        "command": "docker build --platform=linux/amd64 --file apps/backend/competition-service/Migration.Dockerfile --tag blockout-competition-service-migration:local ."
      }
    }
  },
  "tags": ["language:java"]
}
