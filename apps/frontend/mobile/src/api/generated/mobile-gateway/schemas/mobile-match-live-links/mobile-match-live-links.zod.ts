/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * Blockout Mobile Gateway API
 * OpenAPI spec version: 2.0.0
 */
import * as zod from 'zod';

/**
 * Delegates provider, ownership, account-age, quota, timing, professional-league, and state decisions to matches-service.
 * @summary Create or update a mobile match live link
 */

export const UpsertMobileMatchLiveLinkParams = zod.object({
  matchId: zod.number().min(1),
});

export const upsertMobileMatchLiveLinkBodyUrlMax = 1024;

export const UpsertMobileMatchLiveLinkBody = zod
  .object({
    url: zod.string().min(1).max(upsertMobileMatchLiveLinkBodyUrlMax),
  })
  .describe(
    'Mobile live-link input. Provider and every policy decision remain server-owned.',
  );

export const upsertMobileMatchLiveLinkResponseUrlMax = 1024;

export const UpsertMobileMatchLiveLinkResponse = zod
  .object({
    matchId: zod
      .number()
      .min(1)
      .describe('Canonical positive numeric identifier wire shape.'),
    provider: zod
      .enum(['YOUTUBE', 'TWITCH', 'FACEBOOK'])
      .describe('Supported providers for a match live link.'),
    url: zod.string().max(upsertMobileMatchLiveLinkResponseUrlMax),
    status: zod
      .enum([
        'ACTIVE',
        'DEACTIVATED',
        'BANNED',
        'EXPIRED',
        'PENDING',
        'REJECTED',
      ])
      .describe('Moderation and lifecycle states for a match live link.'),
  })
  .describe(
    'Narrow command result retained for generated-client compatibility even though the current Expo mutation ignores its body.',
  );

/**
 * Returns 204 when no active link exists and retains matches-service owner or moderator authorization.
 * @summary Deactivate the active mobile match live link
 */

export const DeleteMobileMatchLiveLinkParams = zod.object({
  matchId: zod.number().min(1),
});

export const DeleteMobileMatchLiveLinkResponse = zod.void();

/**
 * @summary Report the active mobile match live link
 */

export const ReportMobileMatchLiveLinkParams = zod.object({
  matchId: zod.number().min(1),
});

export const reportMobileMatchLiveLinkBodyReasonMin = 10;
export const reportMobileMatchLiveLinkBodyReasonMax = 500;

export const ReportMobileMatchLiveLinkBody = zod
  .object({
    reason: zod
      .string()
      .min(reportMobileMatchLiveLinkBodyReasonMin)
      .max(reportMobileMatchLiveLinkBodyReasonMax),
  })
  .describe(
    'Mobile report explanation using the current submitted-form constraints.',
  );

export const ReportMobileMatchLiveLinkResponse = zod.void();
