global:
  scrape_interval: 15s  # Intervalle par défaut entre les scrapes

scrape_configs:
  - job_name: 'scraper'
    static_configs:
      - targets: ['host.docker.internal:8000']  # Nom du service Docker + port exposé
